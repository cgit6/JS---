// logical operator return boolean value

/**
 * A == B 比較值是否相等
 * A === B 比較值與資料型態是否相等
 * A != Ｂ 兩值是否不相等
 * A !== B 兩值與資料型態是否不相等
 * A > < >= <= B return boolean value
 * */

// and , or
/**
 * A,B 為Boolean value
 * A && B : A and B
 * A || B : A or B
 */
